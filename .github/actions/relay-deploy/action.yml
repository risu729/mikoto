name: Relay Deploy
description: Install tools and deploy the relay Worker with Wrangler.
inputs:
  dry-run:
    description: Run Wrangler production deploy as a dry run.
    required: true
  cloudflare-account-id:
    description: Cloudflare account ID.
    required: false
  cloudflare-api-token:
    description: Cloudflare API token.
    required: false
runs:
  using: composite
  steps:
    - name: Install mise
      uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
      with:
        version: 2026.6.13
      env:
        MISE_ENABLE_TOOLS: bun,node,wrangler
    - name: Dry-run relay deploy
      id: dry-run
      if: inputs.dry-run == 'true'
      shell: bash
      run: mise //packages/relay:deploy:production -- --dry-run
    - name: Require Cloudflare credentials
      id: cloudflare-credentials
      if: steps.dry-run.outcome == 'skipped'
      env:
        CLOUDFLARE_ACCOUNT_ID: ${{ inputs.cloudflare-account-id }}
        CLOUDFLARE_API_TOKEN: ${{ inputs.cloudflare-api-token }}
      shell: bash
      run: |
        if [[ -z "${CLOUDFLARE_ACCOUNT_ID}" || -z "${CLOUDFLARE_API_TOKEN}" ]]; then
          echo "CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_API_TOKEN are required for relay deployment." >&2
          exit 1
        fi
    - name: Deploy relay
      if: steps.cloudflare-credentials.outcome == 'success'
      env:
        CLOUDFLARE_ACCOUNT_ID: ${{ inputs.cloudflare-account-id }}
        CLOUDFLARE_API_TOKEN: ${{ inputs.cloudflare-api-token }}
      shell: bash
      run: mise //packages/relay:deploy:production
