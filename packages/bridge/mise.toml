#:schema https://mise.jdx.dev/schema/mise.json

[tasks.run]
description = "Run the local bridge."
# Keep mikoto.toml and configured backend paths relative to the repository root.
dir = "../.."
run = "bun packages/bridge/src/index.ts"
